meta:
  environment: python
  toolchain:
    name: pip
    version: 3.9
build:
  requirementsPath: requirements.txt
run:
  command: streamlit run ./demo_app/demo_app.py --server.port=80 --server.enableXsrfProtection=false
  persistenceMount: /data
  containerPort: 80
